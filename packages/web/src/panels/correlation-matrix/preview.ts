import type { PanelPreview } from "../_core/types";
import screenshot1 from "./preview/screenshot-1.png";

const preview: PanelPreview = {
  description:
    "Correlation Matrix computes pairwise correlations of daily returns across selected tickers. Green cells indicate positive correlation, red indicates negative, and gray indicates near-zero.",
  assets: [screenshot1],
};

export default preview;
