import type { PanelPreview } from "../_core/types";
import screenshot1 from "./preview/screenshot-1.png";

const preview: PanelPreview = {
  description:
    "Growth Metrics lets you toggle between estimated EPS growth and revenue growth for each ticker. Bars extend right for positive growth and left for negative.",
  assets: [screenshot1],
};

export default preview;
