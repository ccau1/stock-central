import type { PanelPreview } from "../_core/types";
import screenshot1 from "./preview/screenshot-1.png";

const preview: PanelPreview = {
  description:
    "Dividend Yield shows a horizontal bar chart comparing the dividend yield percentage across all selected tickers. Useful for income-focused portfolios.",
  assets: [screenshot1],
};

export default preview;
