import type { PanelPreview } from "../_core/types";
import screenshot1 from "./preview/screenshot-1.png";

const preview: PanelPreview = {
  description:
    "The VIX panel displays the CBOE Volatility Index with current level, directional change, and a 1-year price chart. Sentiment labels help interpret market fear levels.",
  assets: [screenshot1],
};

export default preview;
