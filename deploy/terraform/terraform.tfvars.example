# Copy this to terraform.tfvars and fill in your values
hcloud_token = "YOUR_HETZNER_CLOUD_API_TOKEN"

# Optional overrides
# ssh_public_key_path = "~/.ssh/id_ed25519.pub"
# server_type         = "cx23"
# location            = "nbg1"
# enable_backups      = false

# ── Cloudflare DNS (optional) ─────────────────────────────────
# Get these from your Cloudflare dashboard:
#   - Zone ID: Domain Overview → Zone ID (right sidebar)
#   - API Token: My Profile → API Tokens → Create Token
#     Use the "Edit zone DNS" template, select your domain
#
# cloudflare_api_token = "YOUR_CLOUDFLARE_API_TOKEN"
# cloudflare_proxied   = true
#
# cloudflare_records = {
#   stocks = {
#     name    = "stocks"
#     domain  = "tribalorigin.com"
#     zone_id = "YOUR_TRIBALORIGIN_ZONE_ID"
#     # proxied = true  # optional, defaults to cloudflare_proxied
#   }
#   finanao_apex = {
#     name    = "@"
#     domain  = "finanao.com"
#     zone_id = "YOUR_FINANAO_ZONE_ID"
#   }
# }
